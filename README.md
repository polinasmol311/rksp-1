# LightWeb Studio - Сайт Дизайн-Студии

Полнофункциональное веб-приложение для дизайн-студии с современным адаптивным фронтендом и Django REST API бэкендом с аутентификацией пользователей, управлением заказами и системой тарифов.

## Возможности

### Фронтенд
- 🎨 Современный адаптивный дизайн в зелено-бело-черной цветовой гамме
- 📱 Мобильно-дружественный интерфейс
- 🔐 Аутентификация пользователей (вход/регистрация)
- 👤 Управление профилем пользователя
- 📋 Создание и отслеживание заказов
- 💰 Динамическое отображение тарифов
- 🎯 Плавные анимации и hover-эффекты

### Бэкенд
- 🚀 Django REST Framework API
- 🔑 JWT аутентификация
- 👥 Управление пользователями с кастомной моделью
- 📦 Система управления заказами
- 💎 Управление тарифами
- 📚 Документация API с Swagger/ReDoc
- 🧪 Комплексное покрытие тестами

## Технологический стек

### Фронтенд
- HTML5, CSS3, JavaScript (ES6+)
- Адаптивный дизайн с CSS Grid/Flexbox
- Кастомные CSS анимации
- Fetch API для связи с бэкендом

### Бэкенд
- Python 3.8+
- Django 5.2+
- Django REST Framework
- JWT Аутентификация
- SQLite (разработка) / PostgreSQL (продакшн)
- pytest для тестирования

## Быстрый старт

### Требования
- Python 3.8 или выше
- Git

### Установка

1. **Клонируйте репозиторий**
   ```bash
   git clone <ваш-url-репозитория>
   cd lightweb-studio
   ```

2. **Запустите скрипт установки**
   ```bash
   # На Windows
   setup.bat
   
   # На macOS/Linux
   chmod +x setup.sh
   ./setup.sh
   ```

3. **Запустите сервер разработки**
   ```bash
   cd backend
   
   # Windows
   venv\Scripts\activate
   
   # macOS/Linux
   source venv/bin/activate
   
   python manage.py runserver
   ```

4. **Запустите фронтенд**
   ```bash
   # В новом терминале, из корня проекта
   python -m http.server 3000
   ```

5. **Откройте приложение**
   - Фронтенд: http://localhost:3000
   - Бэкенд API: http://127.0.0.1:8000
   - Документация API: http://127.0.0.1:8000/swagger/

## Структура проекта

```
lightweb-studio/
├── backend/                 # Django бэкенд
│   ├── config/             # Настройки Django
│   ├── users/              # Приложение управления пользователями
│   ├── orders/             # Приложение управления заказами
│   ├── tariffs/            # Приложение управления тарифами
│   ├── manage.py
│   └── requirements.txt
├── css/                    # Стили фронтенда
│   ├── main.css           # Базовые стили
│   ├── header.css         # Стили шапки
│   ├── footer.css         # Стили подвала
│   ├── forms/             # Стили форм
│   └── pages/             # Стили страниц
├── js/                     # JavaScript фронтенда
│   ├── auth.js            # Менеджер аутентификации
│   ├── navigation.js      # Обработчик навигации
│   ├── profile.js         # Логика страницы профиля
│   ├── order.js           # Логика страницы заказа
│   └── home.js            # Логика главной страницы
├── images/                 # Статические изображения
├── fonts/                  # Файлы шрифтов
├── *.html                  # HTML страницы
├── setup.sh               # Скрипт установки (Unix)
├── setup.bat              # Скрипт установки (Windows)
├── .gitignore             # Исключения Git
└── README.md
```

## API Эндпоинты

### Аутентификация
- `POST /api/v1/users/token/` - Получить JWT токен
- `POST /api/v1/users/token/refresh/` - Обновить JWT токен
- `POST /api/v1/users/register/` - Зарегистрировать нового пользователя

### Управление пользователями
- `GET /api/v1/users/me/` - Получить профиль текущего пользователя
- `PUT /api/v1/users/me/update/` - Обновить профиль пользователя
- `DELETE /api/v1/users/me/delete/` - Удалить аккаунт пользователя

### Заказы
- `GET /api/v1/orders/` - Список заказов пользователя
- `POST /api/v1/orders/` - Создать новый заказ
- `GET /api/v1/orders/{id}/` - Получить детали заказа
- `PUT /api/v1/orders/{id}/update/` - Обновить заказ
- `DELETE /api/v1/orders/{id}/delete/` - Удалить заказ

### Тарифы
- `GET /api/v1/tariffs/` - Список доступных тарифов
- `GET /api/v1/tariffs/{id}/` - Получить детали тарифа

## Тестовые данные

После запуска скрипта установки вы можете использовать следующие тестовые аккаунты:

### Админ аккаунт
- **Email:** zemdenalex@gmail.com
- **Пароль:** admin123

### Тестовые пользователи
1. **Иван Петров**
   - Email: ivan.petrov@example.com
   - Пароль: testpass123
   - Компания: ТОО "Альфа"

2. **Мария Сидорова**
   - Email: maria.sidorova@example.com
   - Пароль: testpass123
   - Компания: ИП Сидорова М.А.

3. **Александр Козлов**
   - Email: alex.kozlov@example.com
   - Пароль: testpass123
   - Компания: ООО "Бета Технологии"

### Доступные тарифы
1. **Базовый** - 49 999 ₽
2. **Премиум** - 69 999 ₽
3. **Корпоративный** - 120 000 ₽

## Руководство по функциям

### Регистрация и аутентификация пользователей
1. Перейдите на страницу регистрации
2. Заполните свои данные (имя, email, пароль)
3. Вас автоматически перенаправит на страницу входа
4. Войдите с вашими учетными данными
5. Получите доступ к профилю и истории заказов

### Создание заказов
1. Перейдите в "Заказать сайт"
2. Заполните информацию о компании и контактные данные
3. Выберите подходящий тариф
4. Создайте заказ (требуется вход в систему)
5. Отслеживайте статус заказа в своем профиле

### Управление профилем
1. Откройте свой профиль из навигации
2. Обновите личную информацию
3. Просмотрите историю заказов с прокручиваемой навигацией
4. Безопасно выйдите с подтверждением

## Разработка

### Разработка бэкенда
```bash
cd backend

# Создание и активация виртуального окружения
python -m venv venv
# Windows:
venv\Scripts\activate
# macOS/Linux:
source venv/bin/activate

# Установка зависимостей
pip install -r requirements.txt

# Применение миграций
python manage.py migrate

# Создание суперпользователя
python manage.py createsuperuser

# Создание тестовых данных
python manage.py create_mock_data

# Запуск тестов
pytest

# Запуск сервера разработки
python manage.py runserver
```

### Разработка фронтенда
Фронтенд использует vanilla JavaScript и CSS. Для разработки:

1. Используйте локальный сервер для избежания CORS ошибок:
   ```bash
   python -m http.server 3000
   ```

2. Убедитесь, что Django бэкенд запущен на порту 8000

3. Фронтенд автоматически подключается к бэкенд API

### Добавление новых функций

#### Добавление новой страницы
1. Создайте HTML файл в корневой директории
2. Добавьте соответствующий CSS в `css/pages/`
3. Создайте JavaScript файл в `js/` при необходимости
4. Обновите навигацию в `js/navigation.js`

#### Добавление API эндпоинтов
1. Создайте представления в соответствующем Django приложении
2. Добавьте URL паттерны
3. Обновите фронтенд JavaScript для использования новых эндпоинтов
4. Добавьте тесты для новой функциональности

## Тестирование

### Тесты бэкенда
```bash
cd backend
pytest
```

### Чек-лист ручного тестирования
- [ ] Регистрация пользователя работает
- [ ] Вход/выход пользователя работает
- [ ] Обновления профиля сохраняются корректно
- [ ] Заказы можно создавать и просматривать
- [ ] Тарифы отображаются корректно
- [ ] Навигация работает на всех страницах
- [ ] Адаптивный дизайн работает на мобильных устройствах

## Развертывание

### Развертывание бэкенда
1. Настройте базу данных PostgreSQL
2. Обновите переменные окружения в `.env`
3. Примените миграции: `python manage.py migrate`
4. Соберите статические файлы: `python manage.py collectstatic`
5. Используйте WSGI сервер типа Gunicorn

### Развертывание фронтенда
1. Загрузите файлы на веб-сервер
2. Обновите URL API в `js/auth.js` при необходимости
3. Убедитесь, что настройки CORS разрешают ваш домен

## Переменные окружения

Создайте файл `.env` в директории backend:

```env
DEBUG=True
SECRET_KEY=ваш-секретный-ключ
ALLOWED_HOSTS=localhost,127.0.0.1,вашдомен.com
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# База данных (PostgreSQL для продакшна)
DB_NAME=design_studio_db
DB_USER=postgres
DB_PASSWORD=ваш-пароль
DB_HOST=localhost
DB_PORT=5432

# Настройки JWT
JWT_ACCESS_TOKEN_LIFETIME=60
JWT_REFRESH_TOKEN_LIFETIME=1
```

## Устранение неполадок

### Частые проблемы

1. **CORS ошибки**
   - Убедитесь, что фронтенд запущен через веб-сервер
   - Проверьте настройки CORS в Django settings

2. **Проблемы с аутентификацией**
   - Очистите localStorage браузера
   - Проверьте, не истекли ли JWT токены

3. **Проблемы с базой данных**
   - Примените миграции: `python manage.py migrate`
   - Сбросьте базу данных: `python manage.py flush`

4. **Статические файлы не загружаются**
   - Проверьте пути файлов в HTML
   - Убедитесь, что используете веб-сервер

### Получение помощи
1. Проверьте консоль на JavaScript ошибки
2. Проверьте логи Django на ошибки бэкенда
3. Проверьте API эндпоинты в Swagger документации
4. Протестируйте API эндпоинты напрямую с curl или Postman

## Участие в разработке

1. Сделайте форк репозитория
2. Создайте ветку для новой функции
3. Внесите изменения
4. Добавьте тесты для новой функциональности
5. Отправьте pull request

## Лицензия

Этот проект лицензирован под лицензией MIT.

## Контакты

Для вопросов или поддержки обращайтесь к команде разработки.